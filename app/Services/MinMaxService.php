<?php


namespace App\Services;


class MinMaxService
{
    public function getMininum($value)
    {


        /*let minValue = array[0];
        let minIndex = 0;
        let minFirstPos = array[0];

        for (let i = 0; i < array.length; i++){
            if(array[i] < minValue  && array[i] != 0){
                minValue = array[i];
                minIndex = i;
            }
        }

        if(minValue == array[0]){
            for (let i = 1; i < array.length; i++){
                if(array[i] < minValue){
                    minValue = array[i];
                    minIndex = i;
                }
            }
            array[1] = minValue;
            array[minIndex] = minFirstPos;
        }
        else{
            array[0] = minValue;
            array[minIndex] = minFirstPos;
        }

        console.log(array);
        return array.join("");
        }
        */
    }
}
